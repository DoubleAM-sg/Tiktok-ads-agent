"""One-time OAuth bootstrap for the TikTok Marketing API.

Exchanges the auth_code returned by the TikTok portal redirect for a long-lived
access token + refresh token and writes them into .env.

Placeholder — implementation pending developer app approval.
See CLAUDE.md → "Setup Status" for the end-to-end flow.
"""


def main() -> None:
    raise SystemExit(
        "Not yet implemented. Pending TikTok developer app approval.\n"
        "See CLAUDE.md for the OAuth bootstrap flow."
    )


if __name__ == "__main__":
    main()
